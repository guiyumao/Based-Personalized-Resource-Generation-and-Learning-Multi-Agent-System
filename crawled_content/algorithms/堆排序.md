# 堆排序

> 来源: https://www.runoob.com/w3cnote/heap-sort.html
> 爬取时间: 2026-06-25 13:28:31
> 学科: algorithms | 难度: 进阶
> 标签: 算法, 排序, 堆排序, 进阶

---


堆排序（Heapsort）是指利用堆这种数据结构所设计的一种排序算法。堆积是一个近似完全二叉树的结构，并同时满足堆积的性质：即子结点的键值或索引总是小于（或者大于）它的父节点。堆排序可以说是一种利用堆的概念来排序的选择排序。分为两种方法：

- 大顶堆：每个节点的值都大于或等于其子节点的值，在堆排序算法中用于升序排列；
- 小顶堆：每个节点的值都小于或等于其子节点的值，在堆排序算法中用于降序排列；

堆排序的平均时间复杂度为 Ο(nlogn)。


### 1. 算法步骤

- 创建一个堆 H[0……n-1]；

创建一个堆 H[0……n-1]；

- 把堆首（最大值）和堆尾互换；

把堆首（最大值）和堆尾互换；

- 把堆的尺寸缩小 1，并调用 shift_down(0)，目的是把新的数组顶端数据调整到相应位置；

把堆的尺寸缩小 1，并调用 shift_down(0)，目的是把新的数组顶端数据调整到相应位置；

- 重复步骤 2，直到堆的尺寸为 1。

重复步骤 2，直到堆的尺寸为 1。


### 2. 动图演示


假设有一个待排序的列表 [4, 10, 3, 5, 1]，堆排序的过程如下：

- 构建最大堆：初始列表：[4, 10, 3, 5, 1]。从最后一个非叶子节点开始，逐步调整堆：调整节点5：[4, 10, 3, 5, 1]（无需调整）。调整节点10：[4, 10, 3, 5, 1]（无需调整）。调整节点4：[10, 5, 3, 4, 1]。最终最大堆：[10, 5, 3, 4, 1]。

构建最大堆：

**构建最大堆**
- 初始列表：[4, 10, 3, 5, 1]。

初始列表：[4, 10, 3, 5, 1]。

`[4, 10, 3, 5, 1]`
- 从最后一个非叶子节点开始，逐步调整堆：调整节点5：[4, 10, 3, 5, 1]（无需调整）。调整节点10：[4, 10, 3, 5, 1]（无需调整）。调整节点4：[10, 5, 3, 4, 1]。

从最后一个非叶子节点开始，逐步调整堆：

- 调整节点5：[4, 10, 3, 5, 1]（无需调整）。

调整节点5：[4, 10, 3, 5, 1]（无需调整）。

`5`
- 调整节点10：[4, 10, 3, 5, 1]（无需调整）。

调整节点10：[4, 10, 3, 5, 1]（无需调整）。

`10`
- 调整节点4：[10, 5, 3, 4, 1]。

调整节点4：[10, 5, 3, 4, 1]。

`4`
`[10, 5, 3, 4, 1]`
- 最终最大堆：[10, 5, 3, 4, 1]。

最终最大堆：[10, 5, 3, 4, 1]。

- 交换堆顶元素：将堆顶元素10与最后一个元素1交换，列表变为[1, 5, 3, 4, 10]。堆的大小减 1，已排序部分为[10]。

交换堆顶元素：

**交换堆顶元素**
- 将堆顶元素10与最后一个元素1交换，列表变为[1, 5, 3, 4, 10]。

将堆顶元素10与最后一个元素1交换，列表变为[1, 5, 3, 4, 10]。

`1`
`[1, 5, 3, 4, 10]`
- 堆的大小减 1，已排序部分为[10]。

堆的大小减 1，已排序部分为[10]。

`[10]`
- 调整堆：对新的堆顶元素1进行下沉操作：比较1和其子节点5、3，将1与5交换。列表变为[5, 1, 3, 4, 10]。继续比较1和其子节点4，将1与4交换。列表变为[5, 4, 3, 1, 10]。调整后的堆：[5, 4, 3, 1]。

调整堆：

**调整堆**
- 对新的堆顶元素1进行下沉操作：比较1和其子节点5、3，将1与5交换。列表变为[5, 1, 3, 4, 10]。继续比较1和其子节点4，将1与4交换。列表变为[5, 4, 3, 1, 10]。

对新的堆顶元素1进行下沉操作：

- 比较1和其子节点5、3，将1与5交换。

比较1和其子节点5、3，将1与5交换。

`3`
- 列表变为[5, 1, 3, 4, 10]。

列表变为[5, 1, 3, 4, 10]。

`[5, 1, 3, 4, 10]`
- 继续比较1和其子节点4，将1与4交换。

继续比较1和其子节点4，将1与4交换。

- 列表变为[5, 4, 3, 1, 10]。

列表变为[5, 4, 3, 1, 10]。

`[5, 4, 3, 1, 10]`
- 调整后的堆：[5, 4, 3, 1]。

调整后的堆：[5, 4, 3, 1]。

`[5, 4, 3, 1]`
- 重复步骤：将堆顶元素5与最后一个元素1交换，列表变为[1, 4, 3, 5, 10]。堆的大小减 1，已排序部分为[5, 10]。对新的堆顶元素1进行下沉操作：比较1和其子节点4、3，将1与4交换。列表变为[4, 1, 3, 5, 10]。调整后的堆：[4, 1, 3]。

重复步骤：

**重复步骤**
- 将堆顶元素5与最后一个元素1交换，列表变为[1, 4, 3, 5, 10]。

将堆顶元素5与最后一个元素1交换，列表变为[1, 4, 3, 5, 10]。

`[1, 4, 3, 5, 10]`
- 堆的大小减 1，已排序部分为[5, 10]。

堆的大小减 1，已排序部分为[5, 10]。

`[5, 10]`
- 对新的堆顶元素1进行下沉操作：比较1和其子节点4、3，将1与4交换。列表变为[4, 1, 3, 5, 10]。
- 比较1和其子节点4、3，将1与4交换。

比较1和其子节点4、3，将1与4交换。

- 列表变为[4, 1, 3, 5, 10]。

列表变为[4, 1, 3, 5, 10]。

`[4, 1, 3, 5, 10]`
- 调整后的堆：[4, 1, 3]。

调整后的堆：[4, 1, 3]。

`[4, 1, 3]`
- 继续重复：将堆顶元素4与最后一个元素3交换，列表变为[3, 1, 4, 5, 10]。堆的大小减 1，已排序部分为[4, 5, 10]。对新的堆顶元素3进行下沉操作：比较3和其子节点1，无需交换。调整后的堆：[3, 1]。

继续重复：

**继续重复**
- 将堆顶元素4与最后一个元素3交换，列表变为[3, 1, 4, 5, 10]。

将堆顶元素4与最后一个元素3交换，列表变为[3, 1, 4, 5, 10]。

`[3, 1, 4, 5, 10]`
- 堆的大小减 1，已排序部分为[4, 5, 10]。

堆的大小减 1，已排序部分为[4, 5, 10]。

`[4, 5, 10]`
- 对新的堆顶元素3进行下沉操作：比较3和其子节点1，无需交换。

对新的堆顶元素3进行下沉操作：

- 比较3和其子节点1，无需交换。

比较3和其子节点1，无需交换。

- 调整后的堆：[3, 1]。

调整后的堆：[3, 1]。

`[3, 1]`
- 最终步骤：将堆顶元素3与最后一个元素1交换，列表变为[1, 3, 4, 5, 10]。堆的大小减 1，已排序部分为[3, 4, 5, 10]。堆的大小为 1，排序完成。

最终步骤：

**最终步骤**
- 将堆顶元素3与最后一个元素1交换，列表变为[1, 3, 4, 5, 10]。

将堆顶元素3与最后一个元素1交换，列表变为[1, 3, 4, 5, 10]。

`[1, 3, 4, 5, 10]`
- 堆的大小减 1，已排序部分为[3, 4, 5, 10]。

堆的大小减 1，已排序部分为[3, 4, 5, 10]。

`[3, 4, 5, 10]`
- 堆的大小为 1，排序完成。

堆的大小为 1，排序完成。


## 实例


### 时间复杂度

- 构建最大堆：O(n)。

构建最大堆：O(n)。

- 每次调整堆：O(log n)，总共需要调整 n-1 次。

每次调整堆：O(log n)，总共需要调整 n-1 次。

**每次调整堆**
- 总时间复杂度：O(n log n)。

总时间复杂度：O(n log n)。

**总时间复杂度**

### 空间复杂度

- O(1)，堆排序是原地排序算法，不需要额外的存储空间。

O(1)，堆排序是原地排序算法，不需要额外的存储空间。


### 优缺点

- 优点：时间复杂度稳定为 O(n log n)，适合大规模数据。原地排序，不需要额外的存储空间。

优点：

**优点**
- 时间复杂度稳定为 O(n log n)，适合大规模数据。

时间复杂度稳定为 O(n log n)，适合大规模数据。

- 原地排序，不需要额外的存储空间。

原地排序，不需要额外的存储空间。

- 缺点：不稳定排序算法（可能改变相同元素的相对顺序）。对于小规模数据，性能可能不如插入排序等简单算法。

缺点：

**缺点**
- 不稳定排序算法（可能改变相同元素的相对顺序）。

不稳定排序算法（可能改变相同元素的相对顺序）。

- 对于小规模数据，性能可能不如插入排序等简单算法。

对于小规模数据，性能可能不如插入排序等简单算法。


### 适用场景

- 大规模数据集的排序。

大规模数据集的排序。

- 对性能要求较高的场景。

对性能要求较高的场景。

- 适合内存排序（不适合外部排序）。

适合内存排序（不适合外部排序）。


## 代码实现


### JavaScript


### Python


### Go


### Java


### PHP


### C


### C++


参考文章：


https://github.com/hustcc/JS-Sorting-Algorithm/blob/master/7.heapSort.md


https://zh.wikipedia.org/wiki/%E5%A0%86%E6%8E%92%E5%BA%8F

