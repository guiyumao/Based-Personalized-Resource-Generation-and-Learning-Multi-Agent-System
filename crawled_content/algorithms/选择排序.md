# 选择排序

> 来源: https://www.runoob.com/w3cnote/selection-sort.html
> 爬取时间: 2026-06-25 13:28:22
> 学科: algorithms | 难度: 基础
> 标签: 算法, 排序, 选择排序, 基础

---


选择排序（Selection Sort）是一种简单直观的排序算法，无论什么数据进去都是 O(n²) 的时间复杂度。所以用到它的时候，数据规模越小越好。唯一的好处可能就是不占用额外的内存空间了吧。


选择排序基本思想是每次从待排序的数据中选择最小（或最大）的元素，放到已排序序列的末尾，直到全部数据排序完成。


### 1. 算法步骤

- 初始化：将列表分为已排序部分和未排序部分。初始时，已排序部分为空，未排序部分为整个列表。

初始化：将列表分为已排序部分和未排序部分。初始时，已排序部分为空，未排序部分为整个列表。

**初始化**
- 查找最小值：在未排序部分中查找最小的元素。

查找最小值：在未排序部分中查找最小的元素。

**查找最小值**
- 交换位置：将找到的最小元素与未排序部分的第一个元素交换位置。

交换位置：将找到的最小元素与未排序部分的第一个元素交换位置。

**交换位置**
- 更新范围：将未排序部分的起始位置向后移动一位，扩大已排序部分的范围。

更新范围：将未排序部分的起始位置向后移动一位，扩大已排序部分的范围。

**更新范围**
- 重复步骤：重复上述步骤，直到未排序部分为空，列表完全有序。

重复步骤：重复上述步骤，直到未排序部分为空，列表完全有序。

**重复步骤**

### 2. 动图演示


假设有一个待排序的列表 [64, 25, 12, 22, 11]，选择排序的过程如下：

- 第一轮：未排序部分：[64, 25, 12, 22, 11]。找到最小值11，将其与第一个元素64交换。列表变为[11, 25, 12, 22, 64]。已排序部分：[11]，未排序部分：[25, 12, 22, 64]。

第一轮：

**第一轮**
- 未排序部分：[64, 25, 12, 22, 11]。

未排序部分：[64, 25, 12, 22, 11]。

`[64, 25, 12, 22, 11]`
- 找到最小值11，将其与第一个元素64交换。

找到最小值11，将其与第一个元素64交换。

`11`
`64`
- 列表变为[11, 25, 12, 22, 64]。

列表变为[11, 25, 12, 22, 64]。

`[11, 25, 12, 22, 64]`
- 已排序部分：[11]，未排序部分：[25, 12, 22, 64]。

已排序部分：[11]，未排序部分：[25, 12, 22, 64]。

`[11]`
`[25, 12, 22, 64]`
- 第二轮：未排序部分：[25, 12, 22, 64]。找到最小值12，将其与第一个元素25交换。列表变为[11, 12, 25, 22, 64]。已排序部分：[11, 12]，未排序部分：[25, 22, 64]。

第二轮：

**第二轮**
- 未排序部分：[25, 12, 22, 64]。

未排序部分：[25, 12, 22, 64]。

- 找到最小值12，将其与第一个元素25交换。

找到最小值12，将其与第一个元素25交换。

`12`
`25`
- 列表变为[11, 12, 25, 22, 64]。

列表变为[11, 12, 25, 22, 64]。

`[11, 12, 25, 22, 64]`
- 已排序部分：[11, 12]，未排序部分：[25, 22, 64]。

已排序部分：[11, 12]，未排序部分：[25, 22, 64]。

`[11, 12]`
`[25, 22, 64]`
- 第三轮：未排序部分：[25, 22, 64]。找到最小值22，将其与第一个元素25交换。列表变为[11, 12, 22, 25, 64]。已排序部分：[11, 12, 22]，未排序部分：[25, 64]。

第三轮：

**第三轮**
- 未排序部分：[25, 22, 64]。

未排序部分：[25, 22, 64]。

- 找到最小值22，将其与第一个元素25交换。

找到最小值22，将其与第一个元素25交换。

`22`
- 列表变为[11, 12, 22, 25, 64]。

列表变为[11, 12, 22, 25, 64]。

`[11, 12, 22, 25, 64]`
- 已排序部分：[11, 12, 22]，未排序部分：[25, 64]。

已排序部分：[11, 12, 22]，未排序部分：[25, 64]。

`[11, 12, 22]`
`[25, 64]`
- 第四轮：未排序部分：[25, 64]。找到最小值25，它已经在正确的位置，无需交换。列表保持不变：[11, 12, 22, 25, 64]。已排序部分：[11, 12, 22, 25]，未排序部分：[64]。

第四轮：

**第四轮**
- 未排序部分：[25, 64]。

未排序部分：[25, 64]。

- 找到最小值25，它已经在正确的位置，无需交换。

找到最小值25，它已经在正确的位置，无需交换。

- 列表保持不变：[11, 12, 22, 25, 64]。

列表保持不变：[11, 12, 22, 25, 64]。

- 已排序部分：[11, 12, 22, 25]，未排序部分：[64]。

已排序部分：[11, 12, 22, 25]，未排序部分：[64]。

`[11, 12, 22, 25]`
`[64]`
- 第五轮：未排序部分：[64]。只有一个元素，无需操作。列表完全有序：[11, 12, 22, 25, 64]。

第五轮：

**第五轮**
- 未排序部分：[64]。

未排序部分：[64]。

- 只有一个元素，无需操作。

只有一个元素，无需操作。

- 列表完全有序：[11, 12, 22, 25, 64]。

列表完全有序：[11, 12, 22, 25, 64]。


## 实例


### 时间复杂度

- 最坏情况：O(n²)，无论输入数据是否有序，都需要进行 n(n-1)/2 次比较。

最坏情况：O(n²)，无论输入数据是否有序，都需要进行 n(n-1)/2 次比较。

**最坏情况**
- 最好情况：O(n²)，即使列表已经有序，仍需进行相同数量的比较。

最好情况：O(n²)，即使列表已经有序，仍需进行相同数量的比较。

**最好情况**
- 平均情况：O(n²)。

平均情况：O(n²)。

**平均情况**

### 空间复杂度

- O(1)，选择排序是原地排序算法，不需要额外的存储空间。

O(1)，选择排序是原地排序算法，不需要额外的存储空间。


### 优缺点

- 优点：实现简单，代码易于理解。原地排序，不需要额外的存储空间。对于小规模数据集，性能尚可接受。

优点：

**优点**
- 实现简单，代码易于理解。

实现简单，代码易于理解。

- 原地排序，不需要额外的存储空间。

原地排序，不需要额外的存储空间。

- 对于小规模数据集，性能尚可接受。

对于小规模数据集，性能尚可接受。

- 缺点：时间复杂度较高，不适合大规模数据集。不稳定排序算法（如果存在相同元素，可能会改变它们的相对顺序）。

缺点：

**缺点**
- 时间复杂度较高，不适合大规模数据集。

时间复杂度较高，不适合大规模数据集。

- 不稳定排序算法（如果存在相同元素，可能会改变它们的相对顺序）。

不稳定排序算法（如果存在相同元素，可能会改变它们的相对顺序）。


### 适用场景

- 数据量较小且对性能要求不高的场景。

数据量较小且对性能要求不高的场景。

- 需要简单实现的场景。

需要简单实现的场景。


## 代码实现


### JavaScript 代码实现


### Python 代码实现


### Go 代码实现


### Java 代码实现


### PHP 代码实现


### C 语言


### C++


### C#


### Swift


原文地址：https://github.com/hustcc/JS-Sorting-Algorithm/blob/master/2.selectionSort.md


参考地址：https://zh.wikipedia.org/wiki/%E9%80%89%E6%8B%A9%E6%8E%92%E5%BA%8F

